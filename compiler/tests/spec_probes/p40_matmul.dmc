# Probe 40 — Spec §4.4 / OPERATORS §2.1: matmul @
fn main() -> nil {
    let a: Tensor[i64, [2, 3]] = [[1, 2, 3], [4, 5, 6]]
    let b: Tensor[i64, [3, 2]] = [[1, 2], [3, 4], [5, 6]]
    let c = a @ b
    print(c[0, 0]); print(" ")
    print(c[1, 1]); print("\n")
    nil
}
